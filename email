import groovy.json.JsonSlurper
import jenkins.model.*                                   // Jenkins core class
import com.cloudbees.plugins.credentials.*               // Credentials plugin root
import com.cloudbees.plugins.credentials.domains.*       // Domain
import com.cloudbees.plugins.credentials.impl.*          // File credentials
import hudson.util.*                                     // Utilities

// ===== CONFIGURATION =====
def MOTIO_SERVER = "https://cgrptmcip01.cloud.cammis.ca.gov"
def SOURCE_ENV   = SOURCE_ENV ?: "Cognos-DEV/TEST"
def WORKSPACE    = "/var/lib/jenkins/combined-devops-cognos-deployments/MotioCI/api/CLI"
def PYTHON       = "python3"
def CRED_ID      = "cognos-credentials"
// ==========================

def results = []
def TOKEN = ""

try {
    // --- Step 1️⃣ Get Jenkins Credential Object ---
    results << "🔍 Step1: Looking up Jenkins credential '${CRED_ID}'..."
    def j = Jenkins.getInstance()
    def credsList = j.getExtensionList('com.cloudbees.plugins.credentials.SystemCredentialsProvider')[0]
    def allCreds = credsList.getStore().getCredentials(Domain.global())
    def cred = allCreds.find { it.id == CRED_ID }

    if (!cred) {
        results << "❌ Step1: Credential '${CRED_ID}' not found!"
        return results
    }

    results << "✅ Step1: Credential located successfully (${cred.id})"

    // --- Step 2️⃣ Create temporary credentials file ---
    def tmpCredFile = File.createTempFile("motio-cred-", ".json")
    tmpCredFile.deleteOnExit()
    tmpCredFile.bytes = cred.getContent()
    results << "✅ Step2: Temporary credentials file created at ${tmpCredFile.absolutePath}"

    // --- Step 3️⃣ Verify CLI Path ---
    def cliFile = new File("${WORKSPACE}/ci-cli.py")
    if (!cliFile.exists()) {
        results << "❌ Step3: CLI file not found at ${cliFile.absolutePath}"
        tmpCredFile.delete()
        return results
    }
    results << "✅ Step3: CLI located successfully at ${cliFile.absolutePath}"

    // --- Step 4️⃣ Run login ---
    results << "🔍 Step4: Running login command..."
    def loginCmd = [
        "bash", "-c",
        """
        cd ${WORKSPACE} &&
        ${PYTHON} ci-cli.py --server=${MOTIO_SERVER} \
          login --credentialsFile ${tmpCredFile.absolutePath}
        """
    ]
    results << "🧠 Executing: ${loginCmd.join(' ')}"

    def loginProc = loginCmd.execute()
    def loginOut = new StringBuffer()
    def loginErr = new StringBuffer()
    loginProc.consumeProcessOutput(loginOut, loginErr)
    loginProc.waitFor()

    results << "🔹 Login STDOUT:\n${loginOut.toString()}"
    results << "🔹 Login STDERR:\n${loginErr.toString()}"

    TOKEN = loginOut.toString().find(/[A-Za-z0-9._-]{20,}/)
    if (!TOKEN || TOKEN.length() < 10) {
        results << "❌ Step4: Login failed — token not found!"
        tmpCredFile.delete()
        return results
    }

    results << "✅ Step4: Token captured successfully (${TOKEN.take(10)}...)"

    // --- Step 5️⃣ List projects ---
    results << "🔍 Step5: Fetching projects from MotioCI..."
    def listCmd = [
        "bash", "-c",
        """
        cd ${WORKSPACE} &&
        ${PYTHON} ci-cli.py --server=${MOTIO_SERVER} \
          project ls --xauthtoken ${TOKEN} \
          --instanceName ${SOURCE_ENV}
        """
    ]
    results << "🧠 Executing: ${listCmd.join(' ')}"

    def listProc = listCmd.execute()
    def listOut = new StringBuffer()
    def listErr = new StringBuffer()
    listProc.consumeProcessOutput(listOut, listErr)
    listProc.waitFor()

    def raw = listOut.toString().trim()
    results << "🔹 CLI STDOUT (first 400 chars):\n${raw.take(400)}"
    results << "🔹 CLI STDERR:\n${listErr.toString()}"

    if (!raw) {
        results << "⚠️ Step5: CLI returned empty output — token or env invalid?"
        tmpCredFile.delete()
        return results
    }

    // --- Step 6️⃣ Parse JSON ---
    try {
        results << "🔍 Step6: Parsing JSON response..."
        def json = new JsonSlurper().parseText(raw)
        def edges = json?.data?.instances?.edges ?: []
        def projects = []
        edges.each { e ->
            e?.node?.projects?.edges?.each { p ->
                def name = p?.node?.name
                if (name) projects << name
            }
        }

        if (projects.isEmpty()) {
            results << "⚠️ Step6: JSON parsed but no projects found."
        } else {
            results << "✅ Step6: Parsed successfully — ${projects.size()} projects found."
            results.addAll(projects.unique().sort())
        }
    } catch (Exception pe) {
        results << "❌ Step6: JSON parsing error — ${pe.message}"
        results << "⚠️ Raw JSON snippet:\n${raw.take(500)}"
    }

    tmpCredFile.delete()
    results << "✅ Cleanup complete: temporary credentials removed."

    return results

} catch (Exception e) {
    return ["❌ Unexpected error: ${e.message}", e.toString()]
}
