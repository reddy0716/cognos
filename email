def ENV_ALIAS_MAP = [
  "SBX": "DEV",
  "HFX": "SIT"
]

if (params.DEPLOY_ENV == "NONE") {
  SURGE_ENV = "NONE"
} else {
  SURGE_ENV = ENV_ALIAS_MAP[params.DEPLOY_ENV]
}

echo "User selected label : ${params.DEPLOY_ENV}"
echo "Resolved SURGE_ENV  : ${SURGE_ENV}"
